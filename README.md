Install with:
```
$ git clone git@github.com:Unsolemn/bash-hugo-runner.git
$ cd bash-hugo-runner
$ chmod +x hugoc
$ sudo mv hugoc /usr/bin
```

Run Hugo command:
```
$ hugoc "new site ./my-site"
```
